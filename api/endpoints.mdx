---
sidebar_position: 2
title: "API endpoints"
description: "Complete QWED API endpoint reference covering health checks, verification, batch operations, agent endpoints, observability, and admin routes."
---

## Base URL

```text
https://api.qwedai.com/v1
```

## Health Check

### GET /health

Check API status.

```bash
curl https://api.qwedai.com/v1/health
```

**Response:**

```json
{
  "status": "healthy",
  "version": "1.0.0",
  "timestamp": "2024-12-20T12:00:00Z"
}
```

---

## Verification Endpoints

### POST /verify

Auto-detect and verify any claim.

**Request:**

```json
{
  "query": "Is 2+2=4?",
  "options": {
    "include_attestation": true
  }
}
```

**Response:**

```json
{
  "status": "VERIFIED",
  "verified": true,
  "engine": "math",
  "verification_mode": "SYMBOLIC",
  "result": {
    "is_valid": true
  },
  "attestation": "eyJhbGciOiJFUzI1NiIs..."
}
```

> **`verification_mode`**: Indicates whether the result was proven by a deterministic solver (`SYMBOLIC`) or required LLM fallback (`HEURISTIC`). See [Determinism Guarantee](/advanced/determinism-guarantee) for details.

---

### POST /verify/math

Verify mathematical expressions.

**Request:**

```json
{
  "expression": "x**2 + 2*x + 1 = (x+1)**2"
}
```

---

### POST /verify/logic

Verify logical constraints.

**Request:**

```json
{
  "query": "(AND (GT x 5) (LT y 10))",
  "format": "dsl"
}
```

**Response:**

```json
{
  "status": "VERIFIED",
  "verified": true,
  "verification_mode": "SYMBOLIC",
  "provider_used": "openai_compat",
  "result": {
    "satisfiability": "SAT",
    "model": {"x": 6, "y": 9}
  }
}
```

The `provider_used` field indicates which LLM provider handled the translation step. This field is included in both success and error responses, which helps with debugging provider routing issues.

**Error response:**

```json
{
  "status": "ERROR",
  "error": "Internal verification error",
  "provider_used": "openai_compat"
}
```

---

### POST /verify/code

Check code for security vulnerabilities.

**Request:**

```json
{
  "code": "import os\nos.system('rm -rf /')",
  "language": "python"
}
```

**Response:**

```json
{
  "status": "BLOCKED",
  "verified": false,
  "verification_mode": "SYMBOLIC",
  "result": {
    "vulnerabilities": [
      {
        "type": "os.system",
        "severity": "critical",
        "line": 2,
        "message": "Shell command execution"
      }
    ]
  }
}
```

---

### POST /verify/sql

Validate SQL queries.

**Request:**

```json
{
  "query": "SELECT * FROM users WHERE id = 1",
  "schema": "CREATE TABLE users (id INT, name TEXT)",
  "dialect": "postgresql"
}
```

---

### POST /verify/fact

Verify factual claims against a provided context.

**Request:**

```json
{
  "claim": "Paris is the capital of France",
  "context": "France is a country in Western Europe. Its capital is Paris.",
  "provider": "openai"
}
```

**Response:**

```json
{
  "status": "VERIFIED",
  "verified": true,
  "engine": "fact",
  "result": {
    "is_valid": true,
    "confidence": 0.98,
    "message": "Claim is supported by the provided context"
  }
}
```

---

### POST /verify/consensus

<Info>New in v4.0.0</Info>

Multi-engine consensus verification. Runs the query through multiple verification engines and requires agreement above a confidence threshold.

Mathematical expressions are parsed and compared using `Decimal` arithmetic internally, which avoids floating-point drift when engines cross-check results.

**Request:**

```json
{
  "query": "The square root of 144 is 12",
  "verification_mode": "high",
  "min_confidence": 0.8
}
```

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `query` | string | Yes | The claim to verify |
| `verification_mode` | string | No | `single`, `high`, or `maximum` (default: `single`) |
| `min_confidence` | float | No | Minimum confidence threshold, 0–1 (default: `0.8`) |

**Response:**

```json
{
  "status": "VERIFIED",
  "verified": true,
  "engine": "consensus",
  "result": {
    "confidence": 0.95,
    "engines_used": ["math", "logic"],
    "agreement_rate": 1.0
  }
}
```

---

### POST /verify/image

<Info>New in v4.0.0</Info>

Verify claims about image content. Accepts multipart form data with an image file (max 10 MB).

**Request:**

```bash
curl -X POST https://api.qwedai.com/v1/verify/image \
  -H "X-API-Key: qwed_your_key" \
  -F "image=@photo.jpg" \
  -F "claim=This image contains a cat"
```

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `image` | file | Yes | Image file (max 10 MB) |
| `claim` | string | Yes | The claim about the image to verify |

**Response:**

```json
{
  "status": "VERIFIED",
  "verified": true,
  "engine": "image",
  "result": {
    "is_valid": true,
    "confidence": 0.92,
    "message": "Claim is consistent with image content"
  }
}
```

---

### POST /verify/stats

<Info>New in v4.0.0</Info>

Verify statistical claims against CSV data. Accepts multipart form data with a CSV file.

**Request:**

```bash
curl -X POST https://api.qwedai.com/v1/verify/stats \
  -H "X-API-Key: qwed_your_key" \
  -F "file=@data.csv" \
  -F "query=The average salary is above 50000"
```

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `file` | file | Yes | CSV data file |
| `query` | string | Yes | Statistical claim to verify |

**Response:**

```json
{
  "status": "VERIFIED",
  "verified": true,
  "engine": "stats",
  "result": {
    "is_valid": true,
    "computed_value": 52340.5,
    "message": "Statistical claim verified against dataset"
  }
}
```

---

### POST /verify/process

<Info>New in v4.0.1</Info>

Verify the structural integrity of AI reasoning traces. Supports IRAC structural compliance checking and custom milestone validation with decimal scoring.

**Request (IRAC mode):**

```json
{
  "trace": "The issue is whether the contract was breached. The rule is Article 2 of the UCC. Applying this rule, the defendant failed to deliver on time. In conclusion, breach occurred.",
  "mode": "irac"
}
```

**Request (milestones mode):**

```json
{
  "trace": "Risk assessment complete. Compliance check passed. Implementation timeline defined.",
  "mode": "milestones",
  "milestones": ["risk assessment", "compliance check", "implementation"]
}
```

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `trace` | string | Yes | — | The AI reasoning trace to validate |
| `mode` | string | No | `irac` | `irac` or `milestones` |
| `milestones` | string[] | Conditional | — | Required when `mode` is `milestones` |

**Response (IRAC mode):**

```json
{
  "verified": true,
  "score": 1.0,
  "missing_steps": [],
  "mechanism": "Regex Pattern Matching (Deterministic)"
}
```

**Response (milestones mode):**

```json
{
  "verified": true,
  "process_rate": 1.0,
  "missed_milestones": []
}
```

| Status | Description |
|--------|-------------|
| 400 | Invalid mode or missing `milestones` when `mode` is `milestones` |

---

### POST /verify/rag

<Info>New in v4.0.1</Info>

Verify that retrieved RAG chunks originate from the expected source document. Prevents Document-Level Retrieval Mismatch (DRM) hallucinations in RAG pipelines.

**Request:**

```json
{
  "target_document_id": "contract_nda_v2",
  "chunks": [
    { "id": "c1", "metadata": { "document_id": "contract_nda_v2" } },
    { "id": "c2", "metadata": { "document_id": "contract_nda_v1" } }
  ],
  "max_drm_rate": "0"
}
```

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `target_document_id` | string | Yes | — | Expected source document ID |
| `chunks` | object[] | Yes | — | Array of chunk objects with metadata |
| `max_drm_rate` | string | No | `"0"` | Maximum tolerable mismatch fraction as a `Fraction`-compatible string (e.g. `"0"`, `"1/10"`) |

<Note>`max_drm_rate` accepts only string values for symbolic precision. Use fraction notation like `"1/10"` instead of `0.1`.</Note>

**Response:**

```json
{
  "verified": false,
  "risk": "DOCUMENT_RETRIEVAL_MISMATCH",
  "drm_rate": 0.5,
  "chunks_checked": 2,
  "mismatched_count": 1
}
```

| Status | Description |
|--------|-------------|
| 400 | Invalid request payload (empty `target_document_id`, empty `chunks`, or invalid `max_drm_rate` value) |

---

### POST /verify/batch

Verify multiple items at once.

**Request:**

```json
{
  "items": [
    {"query": "2+2=4", "type": "math"},
    {"query": "3*3=9", "type": "math"},
    {"query": "(GT x 5)", "type": "logic"}
  ],
  "options": {
    "parallel": true,
    "stop_on_failure": false
  }
}
```

**Response:**

```json
{
  "results": [...],
  "summary": {
    "total": 3,
    "verified": 3,
    "failed": 0,
    "success_rate": 100.0
  }
}
```

---

## Agent endpoints

### POST /agents/register

Register a new AI agent with QWED for verified agentic workflows.

**Request:**

```json
{
  "name": "FinanceBot",
  "agent_type": "supervised",
  "description": "Financial analysis agent",
  "permissions": ["math", "logic", "code"],
  "max_cost_per_day": 50.0
}
```

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `name` | string | Yes | — | Agent display name |
| `agent_type` | string | No | `autonomous` | `supervised`, `autonomous`, or `trusted` |
| `description` | string | No | — | Agent description |
| `permissions` | array | No | `[]` | Allowed verification engine names (e.g., `math`, `logic`, `code`) |
| `max_cost_per_day` | float | No | `100.0` | Daily budget cap in USD |

**Response:**

```json
{
  "agent_id": 42,
  "agent_token": "qwed_agent_...",
  "name": "FinanceBot",
  "type": "supervised",
  "status": "active",
  "max_cost_per_day": 50.0,
  "message": "Agent registered successfully. Store the agent_token securely."
}
```

<Warning>Store the `agent_token` immediately — it cannot be retrieved again after registration.</Warning>

---

### POST /agents/{agent_id}/verify

Verify a claim using an agent token. Creates an auditable record tied to the agent. Optionally runs Sentinel Guard security checks (exfiltration detection, MCP poisoning) before verification.

**Request:**

```json
{
  "query": "What is 15% of 200?",
  "provider": "openai",
  "security_checks": {
    "exfiltration": true,
    "mcp_poison": false
  },
  "tool_schema": null
}
```

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `query` | string | Yes | The claim to verify |
| `provider` | string | No | LLM provider preference |
| `security_checks.exfiltration` | boolean | No | Run `ExfiltrationGuard` on the query payload |
| `security_checks.mcp_poison` | boolean | No | Run `MCPPoisonGuard` on the `tool_schema` |
| `tool_schema` | object | No | MCP tool definition to scan (required when `mcp_poison` is `true`) |

**Headers:**

```text
X-Agent-Token: qwed_agent_...
```

**Error responses:**

| Status | Description |
|--------|-------------|
| 400 | Invalid security check configuration or payload |
| 401 | Invalid agent token |
| 403 | Agent budget exceeded or request blocked by security checks |
| 500 | Internal agent verification error |

---

### POST /agents/{agent_id}/tools/{tool_name}

Submit an agent tool call for risk evaluation before execution.

**Request:**

```json
{
  "tool_params": {
    "query": "SELECT * FROM users",
    "dialect": "postgresql"
  }
}
```

---

### GET /agents/{agent_id}/activity

Retrieve the audit log for a specific agent.

**Query params:** `limit` (default: 50)

**Response:**

```json
{
  "agent_id": "agent_abc123",
  "activity": [
    {
      "timestamp": "2026-03-20T12:00:00Z",
      "action": "verify",
      "engine": "math",
      "status": "VERIFIED"
    }
  ]
}
```

---

## Attestation Endpoints

### GET /attestation/:id

Get an attestation by ID.

### POST /attestation/verify

Verify an attestation JWT.

**Request:**

```json
{
  "jwt": "eyJhbGciOiJFUzI1NiIs..."
}
```

---

## Observability endpoints

<Info>New in v4.0.0</Info>

### GET /metrics

Returns global system metrics (request counts, latencies, engine usage).

### GET /metrics/{organization_id}

Returns per-tenant metrics scoped to a specific organization.

### GET /metrics/prometheus

Returns metrics in Prometheus scrape format for integration with monitoring infrastructure.

### GET /logs

Returns verification logs for the authenticated tenant. Supports pagination.

---

## Admin endpoints

<Info>New in v4.0.0</Info>

These endpoints require the `admin:all` API key scope.

### GET /admin/compliance/export/csv

Export the full audit trail as a CSV file.

### GET /admin/compliance/verify/{log_id}

Cryptographically verify a specific audit log entry using HMAC and hash chain validation.

### GET /admin/compliance/report/soc2/{org_id}

Generate a SOC 2 Type II compliance report for an organization.

### GET /admin/security/threats/{org_id}

Returns a real-time threat summary for an organization, including blocked injection attempts and anomalous patterns.

### POST /admin/keys/rotate

Rotate an API key. Available to admin and member roles.

**Request:**

```json
{
  "key_id": "key_abc123"
}
```

---

## Badge endpoints

### GET /badge/verified

Get a verified badge SVG.

### GET /badge/status/:status

Get a badge for any status.

### GET /badge/custom

Generate a custom badge.

**Query params:** `label`, `message`, `color`, `logo`